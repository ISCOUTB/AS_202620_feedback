# Feedback S3 · LostVault

- Avance de última hora registrado: README del esqueleto con comando único (`flutter run` + `flutter test`), prueba automatizada inicial y una integrante más contribuyendo vía PR.
- Sigue pendiente lo principal de S3: la sección 4 y la matriz son genéricas — compárenlas contra SUS escenarios 1-4 (disponibilidad, usabilidad, seguridad, rendimiento), fila por escenario.
- Los paquetes de módulos que declara el ADR (`lib/core`, `lib/features/*`) no existen en el repositorio: solo está `lib/main.dart`. El checklist del README los da por creados.
- Enlacen el ADR desde `aspectos.md` y desde el escenario que lo motiva.
- Actualicen `docs/ia.md` (última entrada 08-ago) con los usos de S3 y lo rechazado con motivo.
- Muevan el C4 a `docs/c4/`, borren los archivos residuales de la raíz (`front_end`, `ejecutable`) y añadan pipeline o evidencia del verde antes del corte 1.
